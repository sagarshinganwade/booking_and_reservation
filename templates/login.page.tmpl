{{template "base" .}}
{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="column">
                <h1>This is Login Page</h1>
                <h1>This Data came from Template Data: {{index .StrMap "test"}}</h1>
            </div>
        </div>
    </div>
{{end}}